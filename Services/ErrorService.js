const ErrorLogModel = require('../Models/ErrorModel')

class ErrorService{
    /**
     * @param text:String - error message,
     * @param serviceName:String - module name error
     * */
    addLog(text, serviceName){

    }
}

module.exports = new ErrorService()